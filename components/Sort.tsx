import React from "react";
import { Button } from "@/components/ui/button";

const Sort = () => {
  return (
    <div>
      <Button variant={"outline"}>Sort</Button>
    </div>
  );
};

export default Sort;
